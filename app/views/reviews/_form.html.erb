<%= form_for(@review) do |f| %>
  <% if @review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% @review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.hidden_field :crash_id %>

  <div class="field">
    <%= f.label :rating, "How would you rate #{@crash.crasher.first_name} as a guest?" %><br>
    <%= f.number_field :rating , max: 5%>(out of 5)
  </div>
  <div class="field">
    <%= f.label :review_text, "Tell us some more about #{@crash.crasher.first_name}\'s stay." %><br>
    <%= f.text_area :review_text, rows: 15, cols: 50 %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
